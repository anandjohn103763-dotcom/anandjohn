# Pulse feedback hub

A lightweight PHP feedback system with a public feedback form and a simple team inbox for filtering and updating feedback statuses. Feedback is stored locally in `storage/feedback.json` so no database setup is required.

## Run locally

```bash
php -S localhost:8000
```

Then visit [http://localhost:8000](http://localhost:8000).
